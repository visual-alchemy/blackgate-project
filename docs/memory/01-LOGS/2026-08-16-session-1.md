# 2026-08-16 Session 1 — ULW-Research: Exhaustive Codebase Map

## Mode
ULW-RESEARCH + ultrawork. 7-axis saturation exploration of blackgate-project @ rust-migration bc90a8f.

## What Done
- Session dir: `.omo/ulw-research/20260816-012111/` — intent-diff, claim-graph (C1-C22 + U1/U2), observation-manifest, verification-economics, expansion-log (convergence declared), SYNTHESIS.md, 7 wave digests.
- 7 background explore workers: native C, Rust workspace, Elixir OTP, Phoenix API, React, packaging/ISO, git drift.

## Key Findings (detail in SYNTHESIS.md)
1. **Rust switch live in working tree**: `route_handler.ex` spawns `blackgate-engine` (Rust), not `blackgate_pipeline` (C). C = legacy ref + archive snapshot. 11 crates ~2,874 LOC **100% untracked, zero commits**.
2. **9 Rust↔C parity gaps**, worst: stats JSON keys diverge (`packets-lost` vs `packets-received-lost`) → consumers (`route_health.ex:97`, `RouteStats.jsx`) read C keys → silent zeros. Multi-sink drop (sinks.first()), sync=false, SDI upmix missing.
3. **Packaging broken (logical)**: `include_erts:false` uncommitted + no Erlang runtime in Docker runner/ISO → release boot likely fails both paths. RELEASE_COOKIE 3-way drift. Untested runtime = U2.
4. **bulk_action bypasses license 402** (gate only in `start`, route_controller.ex:94 vs :202). Verified by orchestrator.
5. **Git state risky**: 15 modified + 1 deleted + ~60 untracked; `router.ex` uncommitted TEMP DEBUG auth token logging — strip before commit; `native/rust/target/` would flood commit; graphify `graph.json` (+26,930) tracked in repo.
6. Docs drift everywhere: AGENTS.md/README stale (React 18→19, Debian→Ubuntu ISO, srt_proxy never existed). 7/9 intent truths violated.
7. Failover engine real: 4 modes maintain-primary/maintain-stability/manual-switchback/manual, gen_statem, watchdog, circuit breaker, dual-ingest input-selector C↔Rust identical IPC.

## Refuted Memory/Doc Claims
- "18 cargo test suites" → 4 `#[test]` (engine-metadata only).
- VICTORIOMETRICS typo → consistent naming.
- Blackgate.Api dead → alive, consumed, 16 tests.
- srt_proxy.c → never committed.
- README API table complete → 5+ endpoints missing.

## Unresolved
- U1: Khepri 0.16.x pin vs lockfile drift unverified.
- U2: Docker/ISO release boot (include_erts:false) — needs runtime test.

## Lessons
- Cancelled+resumed bg tasks: results in `ses_*` transcript (session_read from_end), NOT background_output ("Aborted" misleading).
- Explore workers w/o hard stop order can run 2h+ — set explicit deadline + "write final now" resume.

## Next Steps
1. Commit decision: rust workspace + modified files (strip router.ex debug logging first; gitignore native/rust/target/).
2. Fix parity gap #1 (stats keys) before any Rust cutover.
3. Runtime-verify Docker boot (U2) + khepri pin (U1).
4. Optional: HTML report from SYNTHESIS.md (offered).

---

# Part 3 — Docker E2E Test (srt→srt PROVEN)

## Stack Fixes (working tree, uncommitted)
- mix.exs include_erts:true (runner ships no Erlang).
- Dockerfile runner +ffmpeg.
- docker-compose.yml: bridge net, ports 4000+9000-9500/udp, extra_hosts host.docker.internal, env RELEASE_DISTRIBUTION=name RELEASE_NODE=blackgate@127.0.0.1 (khepri identity long-name match — without it every khepri write hangs).
- .dockerignore NEW.

## E2E VERIFIED (live, in Docker)
- Route srt-srt-docker-test (8cf16f94): srtsrc listener :9000 → srtsink caller srt-recv:9100.
- Sender: ffmpeg testsrc2 1080p30 5M + sine → srt://blackgate:9000.
- Receiver (srt-recv loop container): H264 decoding (mid-GOP PPS errors normal until keyframe).
- Stats live: callers[0] 192.168.107.5 w/ packets-received 12k+, receive-rate 5.5 Mbps, rtt 0.35ms.
- Thumbnail branch: /tmp/blackgate_preview_<id>.jpg written live; GET /preview → 200 image/jpeg.
- PAUSED→PLAYING on first data (live source NO_PREROLL = correct semantics).

## Findings
1. caller-connecting signal: gst emits ONLY if srtsrc authenticate=true. C never set it → C prod never printed SOURCE_VALID from this path either. Rust = exact parity. Research digest "contract" ≠ runtime proof.
2. ffmpeg SRT listener exits on peer drop (I/O error) — test harness fragility, not engine bug.
3. Engine dies when sink DNS unresolvable (state failure exit 1, respawn 10s) — same as C error path.
4. Mac gst 1.28 dev-runtime state failure w/ same pipeline that works on Debian 1.22 — container/prod only; do not debug on Mac.
5. Engine path in release: /app/lib/blackgate-1.0.0/priv/native/build/blackgate-engine.
6. RTMP→SRT: engine has NO rtmpsrc (C parity — never supported). Bridge via ffmpeg (-i rtmp://... -f mpegts srt://gw:port) works as shim.

## Open
- Working tree uncommitted: mix.exs, Dockerfile, docker-compose.yml, .dockerignore (+application.ex, db.ex pre-existing).
- Test containers srt-recv/srt-send (self-terminating loops), route left RUNNING for user play.

---

# Session 1 Part 2 — Rust Migration COMPLETED (same day)

## Mode
ultrawork continuation: "ulw lets finish rust migration all of it" = all 9 parity gaps fixed, full C-contract parity.

## What Done (all files in native/rust/)
- **engine-config**: `sinks: Vec<SinkConfig>` (multi-sink, was sinks.first()), auto_join default TRUE (Elixir contract), SDI full fields (device-number/video-mode/interlaced/width/height/framerate w/ C defaults), extra-props passthrough (PropValue Bool/Int/Double/Str), udpsink host/port direct, percent-decode URIs, empty sinks allowed (dummy removed). 9 tests.
- **engine-stats**: C-exact keys+order (total-bytes-received, packets-received-lost, etc; stream-id/bytes-received/packets-lost GONE), real callers array via GValueArray FFI walk (caller-address → "ip:port"), fallback max/sum logic, multi-sink stats_sink: lines, order primary→secondary→sinks, video-info fields, sdi_video_stats array, SDI_AUDIO_SILENT latch (10s interval), SharedSocket Arc<Mutex<UnixStream>>. serde_json preserve_order for byte-parity key order. 6 tests.
- **engine-gst/builder.rs** (NEW, replaces pipeline/dual_pipeline/sdi_pipeline/source/sink): unified C create_pipeline — named sources "source"/"secondary_source", do-timestamp iff SDI, caller-connecting FFI signal (SOURCE_VALID + stream-id socket write + authenticated=TRUE), selector sync-mode="clock"(=C int 1) + always-ok pads + active sink_0, tee allow-not-linked, ts probe (engine-metadata wired: meta_width/height/frame_rate/interlaced + VIDEO_STREAM_TYPE:<codec|unknown>, 500-attempt cap), per-sink queue2(50MB/3s) + tsparse(alignment=7u32,set-timestamps) + srtsink(sync=TRUE,async=FALSE,wait-for-connection=FALSE), full SDI chains (manual+auto-detect w/ full 15-entry mode table, NTSC 1001 normalize, interlace 2:2 tff, audiomixmatrix GValue 8x2, S16LE/48k/8ch/0xc3f caps, health probes, vrate registry, identity sync=TRUE), thumbnail branch wired, auto_join=false holds secondary NULL.
- **engine-gst/bus.rs**: C bus_callback parity — warning JSON to socket (avdec/va/decodebin/tsdemux), SOURCE_INVALID:primary|secondary attribution, Error → exit path, STATE_CHANGED names, SRT Event + connection-removed.
- **engine-sdi**: string mode nicks (was broken numeric "11"-"20"), full lookup table + tests, safe gst::Array matrix builder, caps builder.
- **engine-failover**: clean API, SOURCE_SWITCHED/SECONDARY_JOINED/SECONDARY_LEFT stdout parity.
- **main.rs**: C startup sequence byte-parity (Connected/Argument/Waiting/Received JSON), PLAYING-failure element-state iteration + exit 1, stdin thread commands (switch-source/join-secondary/leave-secondary/stop-route), bus watch main thread, cleanup + "Socket closed.".
- **engine-shadow**: per-line parse (was broken concat format).
- **native/Makefile**: cp binary to native/build/blackgate-engine (dev spawn path was broken).

## Verification
- cargo build --workspace: 0 errors 0 warnings. cargo test --workspace: **25/25 pass** (config 9, stats 6, metadata 4, sdi 5+1, failover 1... breakdown per suite).
- Smoke test (python fake Elixir socket server + init JSON): **21/21 PASS** — handshake route_id:, all C stdout lines, socket stats C-exact keys AND key order (`{"source":"primary","total-bytes-received":...,"sdi_video_stats":[]}`), dual-ingest + auto_join=false + tsparse + thumbnail verified live.
- mix compile ok; mix test route_handler+route_health **53/53**; full mix 80/118 — 38 failures PRE-EXISTING (Ecto sandbox, Blackgate.Repo never started — dead DataCase tests, unrelated).

## Parity gaps status (9/9 closed)
1. stats keys ✅ 2. multi-sink ✅ 3. sync/tsparse ✅ 4. SDI full chains ✅ 5. SOURCE_INVALID via bus ✅ 6. line order ✅ 7. orphans wired (metadata/thumbnail) ✅ 8. shadow fixed ✅ 9. dead event fns replaced w/ real meta_* ✅

## Commit blockers (UNRESOLVED)
- lib/blackgate_web/router.ex uncommitted TEMP DEBUG auth-token logging — STRIP before commit.
- native/rust/target/ untracked flood — add to .gitignore.
- graphify-out/graph.json tracked (+26,930) churn.
