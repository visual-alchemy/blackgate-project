# Session Log: 2026-06-14 - Session 1

## Sprint Goal & Focus
Implement the high-performance, low-latency browser live preview using WebSocket-MSE (Approach C) on the dashboard with isolated port ranges and $0\%$ idle server CPU usage.

## What Was Accomplished

### 1. GStreamer Native C Engine
* **Modified** [gst_pipeline.c](file:///Users/eldyreynanda/Developer/Antigravity/blackgate-project/native/src/gst_pipeline.c) to parse `"preview_port"` from JSON configuration.
* **Added** `add_preview_branch` which sets up a `tcpserversink` element listening on `127.0.0.1:preview_port`.
* **Configured** a leaky queue (`leaky=2` / GST_QUEUE_LEAK_UPSTREAM) before the sink to instantly drop old buffers when no clients are connected, guaranteeing $0\%$ idle server CPU overhead.

### 2. Elixir OTP Backend
* **Modified** [route_handler.ex](file:///Users/eldyreynanda/Developer/Antigravity/blackgate-project/lib/blackgate/route_handler.ex) to define `@preview_port_range 40000..40999` and implement `find_available_preview_port/0` helper.
* **Initialized** and retained `preview_port` in the state data, passing it inside `send_initial_command` to C pipeline on initial start and subsequent reconnects.
* **Exposed** `get_preview_port/1` call API for the socket channel.
* **Created** [preview_channel.ex](file:///Users/eldyreynanda/Developer/Antigravity/blackgate-project/lib/blackgate_web/channels/preview_channel.ex) which handles subscriptions on `"preview:route_id"`, connects a TCP client socket to GStreamer's local preview port on join, and broadcasts incoming stream data as Base64-encoded channel messages.
* **Modified** [user_socket.ex](file:///Users/eldyreynanda/Developer/Antigravity/blackgate-project/lib/blackgate_web/user_socket.ex) to register `"preview:*"` channel prefix.

### 3. React Frontend & Build Verification
* **Created** [jmuxer.js](file:///Users/eldyreynanda/Developer/Antigravity/blackgate-project/web_app/src/utils/jmuxer.js) locally by downloading and packaging it to support fully offline builds.
* **Modified** [socket.js](file:///Users/eldyreynanda/Developer/Antigravity/blackgate-project/web_app/src/utils/socket.js) to export `getSocket` for external channel joining.
* **Modified** [Dashboard.jsx](file:///Users/eldyreynanda/Developer/Antigravity/blackgate-project/web_app/src/pages/Dashboard.jsx) to import `JMuxer` locally and implement the `<LivePreview>` component within `<RoutePreviewCard>`.
* **Compiled** and verified the entire project locally and remotely on the gateway.
* **Deployed and Restarted:** Synchronized all code updates to the gateway, built a fresh production release on the remote gateway (`MIX_ENV=prod mix release`), and performed a clean restart of `blackgate.service` via systemd.

## Key Decisions & System Constraints Resolved
* **Offline Compatibility:** Avoided npm registry failures by downloading `jmuxer` via curl during development and bundling it as a relative project utility.
* **Isolated Preview Range:** Utilized `@preview_port_range 40000..40999` for preview sockets, completely isolating them from the loopback sidecars to prevent port resource collisions.

## Current Codebase State
* **Branch:** `rtmp-ffmpeg`
* **Created files:**
  * `lib/blackgate_web/channels/preview_channel.ex`
  * `web_app/src/utils/jmuxer.js`
* **Modified files:**
  * `native/src/gst_pipeline.c`
  * `lib/blackgate/route_handler.ex`
  * `lib/blackgate_web/user_socket.ex`
  * `web_app/src/utils/socket.js`
  * `web_app/src/pages/Dashboard.jsx`

## Pending Tasks & Next Milestones
* Log in to the gateway UI and verify live video player smoothness and overlay transitions under active feeds.
