# Session Log: 2026-08-04 (Session 1)

## Sprint Goal & Focus
Diagnose and test HEVC HDR (10-bit 4:2:2) SRT stream stability issues (`EPL Test HDR WF 1`, ports 4002/4003) locally via FFmpeg, SRT CLI, and Docker without breaking production codebase.

## What Was Accomplished
1. **Network Layer Isolation & Empirical Diagnostics**:
   - Tested raw SRT feed directly using `ffprobe` and `ffmpeg` with `-f null -`.
   - Identified that `206.54.2.150:4002` and `4003` deliver raw MPEG-TS streams with heavy packet loss over the international WAN link (`RCV-DROPPED ++SUPPRESSED: 85+` packets, `Packet corrupt in stream 0`).
   - Verified that `tlpktdrop=0` causes pipeline/demuxer stalls waiting for unfulfilled ARQ retransmissions, whereas `tlpktdrop=1` causes reference frame loss (`Could not find ref with POC`).

2. **Docker Local Environment Setup**:
   - Updated `docker-compose.yml` locally for Mac testing with explicit UDP/TCP port mappings (`4000`, `4002/udp`, `15000/udp`, `17000/udp`).
   - Verified local web app and SRT Destination deliver functionality to VLC (`srt://127.0.0.1:15000?mode=caller`).

3. **GStreamer Pipeline Optimizations Researched**:
   - Formulated C engine fixes in `native/src/gst_pipeline.c` (`output-corrupt=FALSE`, `config-interval=-1`, `max-threads=8`, `n-threads=8` on `videoconvert`, `vid_identity sync=FALSE`).

4. **Rollback**:
   - Performed clean `git reset --hard 024c86714c9ba67aa66583d5d9792073fa317004` to leave codebase in clean, unpushed state per user request.

## Key Decisions & Constraints
- **Remote Gateway Constraint**: All code changes performed locally on Mac; no remote production edits or git pushes without explicit approval.
- **Rollback State**: Codebase restored strictly to commit `024c86714c9ba67aa66583d5d9792073fa317004`.

## Current Codebase State
- Clean HEAD at commit `024c86714c9ba67aa66583d5d9792073fa317004`.

## Next Milestones
- Re-apply `native/src/gst_pipeline.c` frame-hold / decoder optimizations when user is ready to test on production machine.
