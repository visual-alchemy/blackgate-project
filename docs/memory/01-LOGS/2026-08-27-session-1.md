# 2026-08-27 Session 1 — Codebase Exploration

## Sprint Goal & Focus

Explore current `rust-migration` codebase, verify architecture against recent memory, and identify active execution paths, documentation drift, test coverage, and maintenance hotspots.

## What Was Accomplished

- Read three latest session logs and checked `docs/memory/00-INBOX/`; no pending inbox notes found.
- Read root and subsystem `AGENTS.md` instructions.
- Mapped Phoenix/OTP control plane, Khepri persistence, per-route supervision, Rust/GStreamer process boundary, Unix-socket telemetry, ETS/PubSub delivery, React UI, Docker release, and Ubuntu ISO packaging.
- Inventoried tests: 118 Elixir tests and 25 Rust unit tests; frontend has linting but no test script.
- Checked branch state, recent migration commits, dependency manifests, local toolchain versions, and current working-tree diff.
- Ran `git diff --check`; no whitespace errors. No build or test suites run during this read-focused exploration.

## Key Decisions & System Constraints Resolved

- Live native engine is Rust `blackgate-engine`; C sources remain legacy/reference material. Several docs still describe C binaries and old paths.
- Production persistence uses Khepri. Ecto/SQLite modules, aliases, and scaffold tests remain in tree but production Repo startup is disabled.
- Current Mac toolchain is Elixir 1.20.2 / OTP 29, while `.tool-versions`, Docker, and documentation target differing Elixir/OTP versions. Existing `application.ex` edit appears to accommodate OTP 29 locally.
- ISO builder now uses Ubuntu 22.04 autoinstall and installs a bare-metal OTP release, despite stale Debian/Docker claims in local docs.
- Remote gateway remains diagnostics-only; edits/builds/restarts belong on local Mac.

## Current Codebase State

- Branch: `rust-migration`, 9 commits ahead of `origin/rust-migration`.
- Pre-existing source edits left untouched:
  - `lib/blackgate/application.ex`
  - `lib/blackgate/db.ex`
- Core runtime path:
  - REST request -> `Blackgate` facade -> partitioned dynamic supervisor -> `RouteHandler`
  - `RouteHandler` -> optional FFmpeg remux sidecar -> Rust `blackgate-engine`
  - Rust GStreamer pipeline -> Unix socket -> `UnixSockHandler` -> ETS stats -> PubSub/WebSocket -> React
- Main complexity hotspots: `route_handler.ex` (~1,688 lines), Rust `builder.rs` (~1,062 lines), stale Ecto scaffold, and conflicting deployment/runtime documentation.

## Pending Tasks & Next Milestones

1. Choose focused follow-up: runtime correctness, test-suite repair, documentation/toolchain alignment, or security/API review.
2. Reconcile Elixir/OTP targets across `.tool-versions`, Dockerfile, AGENTS, and active development environment.
3. Decide whether to remove or isolate legacy Ecto code/tests from Khepri production path.
4. Update native/frontend/ISO docs to Rust, React 19, and Ubuntu bare-metal reality.
5. Review license enforcement consistency for single-route versus bulk starts.

---

## Continuation — Rust Migration Execution (2026-08-27/28)

### Outcome

- Executed approved Rust migration completion plan only on `rust-migration`;
  no other branch touched.
- Production runtime, release, Docker image, CI, docs, and Ubuntu appliance path
  now use Rust `blackgate-engine` only. C engine is checksum-frozen under
  `native/archive/c-engine/` for benchmark comparison only.
- Ubuntu 24.04 LTS amd64 is required platform. Final production image:
  `sha256:5437f4b622ffbd3565c71cfe16c3871d47b1a8573d5060d60bda532e52c2722c`.
- Current exact-HEAD benchmark image:
  `sha256:67c9bc05c31fd50c1b32014a694746984248db065ab034dc2f966221294b96c6`.

### Important Fixes Added During Final Benchmarking

- Fixed Rust thumbnail worker shutdown: interruptible five-second wait now
  wakes immediately, preventing benchmark/IPC forced termination. Regression
  test proves shutdown under 500 ms; full SRT runs exit 0 without force.
- Corrected benchmark throughput extraction to use actual source receive rate
  or sink send-rate fallback, never SRT capacity bandwidth.
- Corrected benchmark methodology: unmeasured preflight, interleaved C/Rust
  repetitions, CPU/RSS measurement after warm-up only, and no teardown time in
  CPU denominator.
- Added `--repetitions` for mandatory inconclusive reruns and baked commit/image
  identity in local/CI evidence.

### Fresh Verification

- Runtime/toolchain guard tests and live guards: PASS.
- Rust fmt, Clippy `-D warnings`, workspace tests: PASS (27 tests).
- Native release build and real IPC smoke: PASS.
- Elixir format, warnings-as-errors compile, tests: PASS (101 tests).
- Benchmark Python suite: PASS (21 tests after review fix).
- Frontend production build: PASS. Lint remains unchanged authorized baseline:
  110 errors and 8 warnings; migration did not edit frontend behavior.
- Local OTP release inspection: PASS; only Rust engine present.
- Production image: Ubuntu 24.04 x86_64; only Rust engine; DeckLink plugin
  dependencies all resolved.

### Performance Evidence and Blocker

- Committed seven-repetition dual-ingest evidence at
  `benchmarks/migration/results/dual-methodology-7/`.
- Every median threshold passes: throughput 0.999852, loss delta 0, CPU 0.971091,
  RSS 1.006059, latency 1.035714, startup 1.063382, failover 0.973023.
- Decision remains `INCONCLUSIVE`: CPU, latency, and startup coefficient of
  variation exceeds fixed 5% limit (8.9%–77.9%). Evidence identifies Apple CPU
  behind x86_64 emulation. Policy forbids manual override.
- Status remains `IN_PROGRESS`. Full six-workload suite and one-hour-per-engine
  soak must run on native Ubuntu 24.04 amd64 before `SDI_MANUAL_PENDING`.
  Physical DeckLink/SDI validation remains final manual gate after automated
  performance passes.

### Final Commits Added in Continuation

- `62445f6 fix(engine): bound thumbnail shutdown`
- `b46c273 feat(perf): support stable reruns`
- `37ae513 fix(perf): remove benchmark timing bias`
- `6cb052d build(perf): cache Noble runtime layer`
- `f12ad28 perf: record inconclusive amd64 evidence`
- `22b90d9 ci(perf): record benchmark identity`
