# Session Log: 2026-08-03-session-1

## 📋 Session Overview
- **Topic:** Reviewing `feat/srt-failover` feature branch, resolving codebase misconfigurations, and executing Docker failover integration test on local Mac.
- **Goal:** Comprehensive feature review, error fix, and Docker integration testing.

---

## 🔍 Codebase Review & Applied Fixes

### 1. Feature Review (`feat/srt-failover`)
- **4-Mode Failover Engine**: `none`, `main_backup`, `primary_priority`, `manual`.
- **GStreamer `input-selector` Native C Pipeline**: Single C process receiving primary (`:12100`) and secondary (`:12101`) SRT streams with in-process zero-glitch source switching via stdin commands (`switch-source`, `join`, `leave`).
- **Elixir OTP `RouteHandler`**: In-process Port command source switching, circuit breaker crash counter reset, and dual stream telemetry broadcast over WebSockets.
- **React Web UI**: Added failover configuration card, primary/secondary source tabs, real-time `VALID`/`INVALID` health indicators, and header switch actions.

### 2. Bugs Resolved
- **`lib/blackgate/route_handler.ex`**: Replaced `:error` severity level with `:critical` in `Blackgate.EventLog.log` within `failover_both_invalid` handler. Prevented `FunctionClauseError` process crash when both streams drop.
- **`lib/blackgate/route_handler.ex`**: Resolved compiler wildcard pattern match warnings in `evaluate_failover` calls.
- **`Dockerfile`**: Configured `HEX_HTTP_TIMEOUT="120"` and `HEX_UNSAFE_HTTPS="1"` to resolve container builder package fetch timeouts.

---

## 🧪 Verification & Local Docker Test Results

1. **Unit Tests**:
   - `mix test test/blackgate/route_handler_test.exs test/blackgate/route_health_test.exs` ran 53/53 tests PASSED (100% success).

2. **Docker Failover Integration Scenario (`docker-compose.failover.yml`)**:
   - Launched Blackgate + 2 FFmpeg emitters (`emitter-a` on 12100, `emitter-b` on 12101).
   - Created test route `failover-test` with `failover_mode: maintain-stability`.
   - **Manual Switching Test**: Triggered `POST /api/routes/:id/switch-source` (`primary` <-> `secondary`). Verified in-process switch response `{"status": "switched"}`.
   - **Automatic Failover Test**: Stopped `emitter-a` (`docker stop`). Blackgate detected `SOURCE_INVALID:primary` and automatically switched to `secondary` (`SOURCE_SWITCHED:secondary`) within **4 milliseconds**.
   - Cleanup completed cleanly (`docker compose down`).

---

## 🎨 Failover Diagnostics & UI Polish (Session 2)

1. **SRT Failover Diagnostics**:
   - Identified root cause for UI revert on refresh: watchdog auto-switching back to secondary when primary is evaluated as `:invalid` in `maintain-primary` / `maintain-stability` modes.
   - Identified root cause for VLC display lag: H.264 keyframe (IDR frame) requirement on input pad switch.
   - Fixed `data.route` active source synchronization in `lib/blackgate/route_handler.ex` during manual switch.

2. **UI Enhancements**:
   - **Failover Mode Tooltip (`?` Icon)**: Added help tooltip in `RouteSourceEdit.jsx` detailing all 4 failover modes.
   - **StreamID Details**: Added Primary Stream ID & Secondary Stream ID in `RouteItem.jsx` Source Configuration card.
   - **Dual Source Statistics**: Updated `RouteStats.jsx`, `useRouteStats.js`, and `route_controller.ex` to display side-by-side live stats for both Primary & Secondary sources.
   - **Eliminated Stats Blinking**: Fixed `useRouteStats.js` PubSub listener to prevent nullifying primary stats on secondary stats broadcast.

3. **Build & Test Verification**:
   - React Vite Build: **PASSED (100%)** (`dist/assets/index-DdcKu3bi.js`).
   - Native C Engine Build: **PASSED (100%)** (`make -C native`).
   - Elixir Unit Tests: **53/53 PASSED (100%)**.

4. **Resolution & Framerate Fixes**:
   - **50fps Framerate Inference**: Updated `parse_hevc_sps` in `native/src/gst_pipeline.c` to default HD/UHD HEVC streams (height >= 720) to `50 fps` standard instead of `25 fps`.
   - **Secondary Source Resolution & Framerate**: Attached `video-width`, `video-height`, `video-framerate-num`, `video-framerate-den`, and `video-interlace-mode` to `secondary` stats JSON in `print_stats()` in `native/src/gst_pipeline.c`. Listed Resolution, Framerate, and Scan columns for both Primary and Secondary stats cards in `RouteStats.jsx`.
