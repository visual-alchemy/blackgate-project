#!/bin/sh

# Use NODE_IP if set, otherwise default to 127.0.0.1
# Using IP address avoids FQDN hostname resolution issues
if [ -z "$NODE_IP" ]
then
  ip="127.0.0.1"
else
  ip=$NODE_IP
fi

node_name="${NODE_NAME:=blackgate}"

export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=$node_name@$ip
